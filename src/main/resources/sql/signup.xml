<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="signup">
  
	<insert id="signup">
		INSERT INTO User
			(id, pwd, email, u_delYN, u_manager, u_date)
		 VALUES
			(#{id},#{pwd},#{email},#{u_delYN},#{u_manager},#{u_date});
	</insert>
  
 </mapper>